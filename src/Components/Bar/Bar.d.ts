type Props = {
  /** You get a list of available props by pressing Ctrl+Space while inspecting
   * `<Bar />` in Foo.js, but won’t be able to enjoy the same functionality in a
   * Bar.js itself.
   */
  readMe: boolean
}

export declare function Bar(props: Props): React.FunctionComponent
